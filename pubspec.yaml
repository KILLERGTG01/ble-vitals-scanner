name: ble_scanner
description: BLE device scanner — discover, connect, and stream live data.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_reactive_ble: ^5.3.2
  flutter_bloc: ^9.0.0
  flutter_hooks: ^0.21.0
  go_router: ^14.6.3
  permission_handler: ^11.3.1
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
