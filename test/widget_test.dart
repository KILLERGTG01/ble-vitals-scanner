// Smoke test placeholder — BLE integration tests require a real device.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('smoke test placeholder', (WidgetTester tester) async {
    // BLE operations require a physical device; no widget smoke test here.
    expect(true, isTrue);
  });
}
